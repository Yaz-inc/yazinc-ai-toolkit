# Scripts

Optional automation for your workflow.

Examples:

- `link-skills.ps1` — symlink `skills/` into `~/.cursor/skills/yazinc`
- `push-both-repos.ps1` — parameterized dual GitHub push (no secrets in repo)

Store tokens in local `_deploy/key.git.md` (gitignored) on each machine, not in this repo.
