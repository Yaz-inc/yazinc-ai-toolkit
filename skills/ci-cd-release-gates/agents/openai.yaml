interface:
  display_name: "CI CD Release Gates"
  short_description: "Design safe tested release automation gates"
  default_prompt: "Use $ci-cd-release-gates to design a secure release pipeline with rollback gates."
