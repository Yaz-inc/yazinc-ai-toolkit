# Templates

Copy-paste starters for new projects. Generalize before adding — no client URLs, keys, or business-specific field names.

Examples to add over time:

- `fix.php.example` — post-deploy cache clear
- Controller + DataTable index stub
- Activity logger usage snippet
- Dual-repo push script
